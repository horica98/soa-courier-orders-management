import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.image.search.nike',
  appName: 'nike image search shop',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
